# Copy this to .env and fill values
# Firebase RTDB base URL, without trailing slash
FIREBASE_DATABASE_URL=
# Path to listen to, e.g. /messages or /
FIREBASE_PATH=/

# Provide one of the following authentication methods:
# 1) Path to a Google service account JSON file (recommended for servers)
GOOGLE_SERVICE_ACCOUNT_FILE=
# 2) Or a pre-fetched OAuth access token (useful for quick tests)
GOOGLE_ACCESS_TOKEN=

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# If set to 1, outputs JSON-like log lines
LOG_JSON=0
